# 1 конфигурация глобальное имя
git config --global user.name "Pavel Shift"
# 2 конфигурация почта
git config --global user.mail "shift.pavel@mail.ru"

# 3 инициализация
git init

# история коммитов
git log
# + изменений
git log -p

# 4 статус файлов
git status

# 5 добавление файла под версионный контроль
git add git.txt

# 6 коммит
git commit -m "Старт проекта"
# + взять все файлы под версионным контролем
git commit -am "Старт проекта"


# показывает подробно
git show

# возврашение к последнему коммиту
git restore

# показывает разницу в файле с последнего коммита
git diff
# + в индексе
git diff --staged

# переименование или перемещение файлов
git mv file1 file2
# удаление из идекса
git rm file1

# 7 игнорирование файлов 
-делаем файл .gitignore

# Смена пользователя
git config --global credential.username "Pavel Shift"

# 1 подключение репозитория
git remote add origin ссылка

# 2 просмотр текущих репозиториев
git remote -v

# 3 Получение из репозитория
git pull

# 4 Отправка изменений в репозиторий
git push -u origin main

# 1 Показ существующих веток
git branch

# 2 Новая ветка
git branch new

# 3 Переход на ветку
git checkout new

# 4 Объединение веток
git merge new

# 5 Удаление ветки
git branch -D new

# 6 Перемещение указателя
git reset --hard 3473547...

ЏаЁўҐв